{
  "name": "LEOS Build Environment",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "remoteUser": "root",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-vscode.cpptools",
        "13xforever.language-x86-64-assembly"
      ]
    }
  },
  "postCreateCommand": "pacman -Syu --noconfirm && echo 'LEOS build environment ready. Run: cd archlive && sudo mkarchiso -v -w /tmp/leos-work -o /tmp/leos-out .'"
}
